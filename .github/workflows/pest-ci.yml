<<<<<<< Updated upstream
# Reusable Pest CI suite — runs on believe-vps (not ubuntu-latest).
# Uses ephemeral SQLite only — never production/staging MySQL.
=======
# Reusable Pest CI suite — GitHub-hosted ubuntu + ephemeral SQLite only (never VPS MySQL).
# Kept on ubuntu-latest so Tests are not blocked when the self-hosted runner cannot acquire jobs.
>>>>>>> Stashed changes
name: Pest CI (reusable)

on:
  workflow_call:

jobs:
  pest:
<<<<<<< Updated upstream
    name: Pest CI suite (PHP 8.4 / SQLite on believe-vps)
    runs-on: [self-hosted, believe-vps]
=======
    name: Pest CI suite (PHP 8.4 / SQLite)
    runs-on: ubuntu-latest
>>>>>>> Stashed changes
    timeout-minutes: 25

    env:
      APP_ENV: testing
      DB_CONNECTION: sqlite
      DB_DATABASE: ${{ github.workspace }}/database/ci-testing.sqlite
      CACHE_STORE: array
      QUEUE_CONNECTION: sync
      SESSION_DRIVER: array
      MAIL_MAILER: array
      BROADCAST_CONNECTION: log
      PHP_VERSION: '8.4'

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
<<<<<<< Updated upstream
          fetch-depth: 1

      - name: Use EA-PHP CLI (same as deploy)
        run: |
          set -euo pipefail
          PHP_CLI="/opt/cpanel/ea-php$(echo "${PHP_VERSION}" | tr -d '.')/root/usr/bin/php"
          if [ ! -x "${PHP_CLI}" ]; then
            echo "::error::Missing EA PHP CLI at ${PHP_CLI}"
            exit 1
          fi
          mkdir -p "${HOME}/bin"
          ln -sfn "${PHP_CLI}" "${HOME}/bin/php"
          export PATH="${HOME}/bin:$(dirname "${PHP_CLI}"):${PATH}"
          if [ ! -x "${HOME}/bin/composer" ]; then
            curl -sS https://getcomposer.org/installer | "${PHP_CLI}" -- --install-dir="${HOME}/bin" --filename=composer.phar
            printf '%s\n' '#!/usr/bin/env bash' "exec \"${PHP_CLI}\" \"\${HOME}/bin/composer.phar\" \"\$@\"" > "${HOME}/bin/composer"
            chmod +x "${HOME}/bin/composer"
          fi
          echo "${HOME}/bin" >> "${GITHUB_PATH}"
          echo "$(dirname "${PHP_CLI}")" >> "${GITHUB_PATH}"
          php --version
          php -m | grep -qi pdo_sqlite || { echo "::error::pdo_sqlite required"; exit 1; }
=======
          php-version: '8.4'
          extensions: mbstring, dom, fileinfo, cURL, bcmath, intl, gd, zip, pdo_sqlite, sqlite3, redis, pcntl, posix, sodium
          coverage: none
          tools: composer:v2
>>>>>>> Stashed changes

      - name: Get Composer cache dir
        id: composer-cache
        run: echo "dir=$(composer config cache-files-dir)" >> "$GITHUB_OUTPUT"

      - name: Cache Composer
        uses: actions/cache@v4
        with:
          path: ${{ steps.composer-cache.outputs.dir }}
          key: ${{ runner.os }}-selfhosted-php8.4-composer-${{ hashFiles('composer.lock') }}
          restore-keys: |
            ${{ runner.os }}-selfhosted-php8.4-composer-

      - name: Install dependencies
        run: composer install --prefer-dist --no-interaction --no-progress --optimize-autoloader --ignore-platform-req=ext-sodium

      - name: Prepare application (ephemeral SQLite)
        run: |
          set -euo pipefail
          cp .env.example .env
          mkdir -p storage/framework/{cache,sessions,views} storage/logs bootstrap/cache database
<<<<<<< Updated upstream
          # Empty SQLite file is enough for CI (no migrate:fresh; health check only needs select 1).
=======
>>>>>>> Stashed changes
          : > database/ci-testing.sqlite
          chmod -R ug+rwx storage bootstrap/cache database
          php artisan key:generate --force
          php artisan config:clear

      - name: Run Pest CI suite
        run: php artisan test --compact --testsuite=CI
