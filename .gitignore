/.phpunit.cache
/bootstrap/ssr
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/app
/storage/framework
/storage/logs
/storage/pail
/vendor
.env
.env.backup
.env.production
.azure.env
.phpactor.json
.phpunit.result.cache
COMMAND_IN_PRODUCTION.md
# Full docs folder – do not push
docs/

# Local project overview (never commit — keep outside repo or ignored)
AGENTS.md
PROJECT-OVERVIEW.md
**/AGENTS.md
**/PROJECT-OVERVIEW.md

# Local Apache / cPanel docroot rewrite + PHP handler (never commit)
# Do not commit root `.htaccess` — it is machine/server specific.
/.htaccess
# Prefer not staging any .htaccess; Laravel `public/.htaccess` stays tracked only if already in git — agents must never add or update .htaccess in commits.
**/.htaccess
!public/.htaccess
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed
*.zip

# Local deploy SSH keys (never commit)
/composer
/.ssh-local/

# ForgeStack local dev (machine-specific nginx paths, not for server)
/forgestack/
.env.server
